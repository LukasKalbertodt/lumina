language: cpp
compiler: clang

before_install:
- sudo apt-get update -q
- sudo apt-get install -y libglew1.6dev libglfw-dev

script: sh releasebuild.sh

notifications:
  recipients:
    - lukas.kalbertodt@gmail.com
  email:
    on_success: change
    on_failure: always